// condicionales

/**
 * > mayor
 * < menor
 * >= mayor igual
 * <= menor igual
 * = asignacion
 * == compara el valor
 * === compara el valor y el tipo de datos
 */
let edad = prompt("Ingresa tu edad");

if (edad >= "16") {
    alert("Tiene acceso al catalogo")
} else {
    alert("El catalogo esta bloqueado")
}